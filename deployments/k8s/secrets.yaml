apiVersion: v1
kind: Secret
metadata:
  name: pollpulse-secrets
type: Opaque
stringData:
  # These values should be replaced with actual secrets in a production environment
  # This is just a template
  postgres-user: "postgres"
  postgres-password: "postgres-password"
  jwt-secret: "jwt-secret-key-replace-in-production" 